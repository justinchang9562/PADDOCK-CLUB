# PADDOCK INDEX 贡献指南

## 产品规则

- 产品必须保持中英文双语。所有面向用户的文案都需要同时提供简体中文和英文版本。
- 默认展示当前 F1 赛季，同时保持历史赛季地址稳定且可分享。
- 不得把估算或无法取得的赛车性能数据当作事实；缺失数值必须清楚标注。
- 资讯卡片只包含标题、简短摘要、发布者和外部来源链接。
- 访客收藏只保存在当前浏览器；登录后才允许合并到该用户的 Supabase 收藏，且成功迁移后必须清除匿名副本，避免跨账户混入。
- 保持 `src/lib/providers.ts` 的数据供应商边界，不要让 UI 组件直接依赖上游 API 的响应结构。

## 工程规则

- 优先使用 React Server Components。只有需要浏览器状态或直接交互时才添加 `"use client"`。
- 页面必须支持键盘操作、尊重“减少动态效果”设置，并保留清晰可见的焦点状态。
- 交付候选版本前必须运行 `npm run check`。
- 新增真实摄影图片时，必须同步在 `docs/IMAGE-CREDITS.md` 中补充署名信息。
- 不得提交密钥或受版权保护的 Formula 1 转播及视频素材。
